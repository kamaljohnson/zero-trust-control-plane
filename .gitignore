# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work
go.work.sum

# Dependency directories
vendor/

# Go build artifacts
/bin/
/dist/
/build/

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment variables
.env
.env.local
.env.*.local
*.env

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Node.js (frontend has its own .gitignore, but adding common root-level ignores)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
dist/
build/
out/

# Coverage reports
coverage/
*.coverprofile

# Database files
*.db
*.sqlite
*.sqlite3

# Configuration files with secrets
config.local.*
secrets.yaml
secrets.yml

# Local Compose overrides (use docker-compose.override.example.yml as template)
deploy/docker-compose.override.yml

# PostgreSQL SSL certificates (generate with deploy/scripts/generate-postgres-ssl.sh)
deploy/postgres-ssl/

.cursor/
.nvmrc

backend/server
backend/worker
backend/migrate
backend/seed
.ssh

.env.*
# Exception: keep example template files for developers
!.env*.example
keys/
nginx/
# Exception: keep nginx config files and initial status files
!nginx/conf.d/
!nginx/Dockerfile
!nginx/nginx.conf
!nginx/status.html
!nginx/status.json
# Note: status.json is tracked as a template but will be overwritten at runtime via volume mount