// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: session.sql

package gen

import (
	"context"
	"database/sql"
	"time"
)

const createSession = `-- name: CreateSession :one
INSERT INTO sessions (id, user_id, org_id, device_id, expires_at, revoked_at, last_seen_at, ip_address, created_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, user_id, org_id, device_id, expires_at, revoked_at, last_seen_at, ip_address, created_at
`

type CreateSessionParams struct {
	ID         string
	UserID     string
	OrgID      string
	DeviceID   string
	ExpiresAt  time.Time
	RevokedAt  sql.NullTime
	LastSeenAt sql.NullTime
	IpAddress  sql.NullString
	CreatedAt  time.Time
}

func (q *Queries) CreateSession(ctx context.Context, arg CreateSessionParams) (Session, error) {
	row := q.db.QueryRowContext(ctx, createSession,
		arg.ID,
		arg.UserID,
		arg.OrgID,
		arg.DeviceID,
		arg.ExpiresAt,
		arg.RevokedAt,
		arg.LastSeenAt,
		arg.IpAddress,
		arg.CreatedAt,
	)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.OrgID,
		&i.DeviceID,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.LastSeenAt,
		&i.IpAddress,
		&i.CreatedAt,
	)
	return i, err
}

const getSession = `-- name: GetSession :one
SELECT id, user_id, org_id, device_id, expires_at, revoked_at, last_seen_at, ip_address, created_at
FROM sessions
WHERE id = $1
`

func (q *Queries) GetSession(ctx context.Context, id string) (Session, error) {
	row := q.db.QueryRowContext(ctx, getSession, id)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.OrgID,
		&i.DeviceID,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.LastSeenAt,
		&i.IpAddress,
		&i.CreatedAt,
	)
	return i, err
}

const listSessionsByUserAndOrg = `-- name: ListSessionsByUserAndOrg :many
SELECT id, user_id, org_id, device_id, expires_at, revoked_at, last_seen_at, ip_address, created_at
FROM sessions
WHERE user_id = $1 AND org_id = $2 AND revoked_at IS NULL
ORDER BY created_at
`

type ListSessionsByUserAndOrgParams struct {
	UserID string
	OrgID  string
}

func (q *Queries) ListSessionsByUserAndOrg(ctx context.Context, arg ListSessionsByUserAndOrgParams) ([]Session, error) {
	rows, err := q.db.QueryContext(ctx, listSessionsByUserAndOrg, arg.UserID, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Session
	for rows.Next() {
		var i Session
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.OrgID,
			&i.DeviceID,
			&i.ExpiresAt,
			&i.RevokedAt,
			&i.LastSeenAt,
			&i.IpAddress,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeSession = `-- name: RevokeSession :one
UPDATE sessions
SET revoked_at = $2
WHERE id = $1
RETURNING id, user_id, org_id, device_id, expires_at, revoked_at, last_seen_at, ip_address, created_at
`

type RevokeSessionParams struct {
	ID        string
	RevokedAt sql.NullTime
}

func (q *Queries) RevokeSession(ctx context.Context, arg RevokeSessionParams) (Session, error) {
	row := q.db.QueryRowContext(ctx, revokeSession, arg.ID, arg.RevokedAt)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.OrgID,
		&i.DeviceID,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.LastSeenAt,
		&i.IpAddress,
		&i.CreatedAt,
	)
	return i, err
}

const updateSessionLastSeen = `-- name: UpdateSessionLastSeen :one
UPDATE sessions
SET last_seen_at = $2
WHERE id = $1
RETURNING id, user_id, org_id, device_id, expires_at, revoked_at, last_seen_at, ip_address, created_at
`

type UpdateSessionLastSeenParams struct {
	ID         string
	LastSeenAt sql.NullTime
}

func (q *Queries) UpdateSessionLastSeen(ctx context.Context, arg UpdateSessionLastSeenParams) (Session, error) {
	row := q.db.QueryRowContext(ctx, updateSessionLastSeen, arg.ID, arg.LastSeenAt)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.OrgID,
		&i.DeviceID,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.LastSeenAt,
		&i.IpAddress,
		&i.CreatedAt,
	)
	return i, err
}
