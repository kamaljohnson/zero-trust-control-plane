# Backend gRPC server address (host:port). Used by Next.js API routes (BFF) to call AuthService, DevService, etc.
# Default: localhost:8080. Use TLS (https://...) in production.
BACKEND_GRPC_URL=localhost:8080

# Optional: default organization ID for the login form (single-tenant). Leave unset to show empty field.
# NEXT_PUBLIC_DEFAULT_ORG_ID=my-org-id

# When true, BFF allows GET /api/dev/mfa/otp (server-side). Set only in development; route returns 404 when NODE_ENV is production.
DEV_OTP_ENABLED=false

# When true, login page fetches OTP from GET /api/dev/mfa/otp when MFA step is shown (client-side). Set only in development.
NEXT_PUBLIC_DEV_OTP_ENABLED=false
