# Local dev env template. Copy to backend/.env and optionally to frontend/.env.
# make env (or make setup) copies this to backend/.env and frontend/.env if missing.
#
# --- backend (backend/.env) ---
GRPC_ADDR=:8080
DATABASE_URL=postgres://ztcp:ztcp@localhost:5432/ztcp?sslmode=disable

# Auth: set JWT_PRIVATE_KEY and JWT_PUBLIC_KEY for login/register. See backend/README.md and docs-site/docs/backend/auth.md.
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
JWT_ISSUER=ztcp-auth
JWT_AUDIENCE=ztcp-api
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h
BCRYPT_COST=12

SMS_LOCAL_API_KEY=
SMS_LOCAL_SENDER=
SMS_LOCAL_BASE_URL=https://app.smslocal.in/api/smsapi
DEFAULT_TRUST_TTL_DAYS=30
APP_ENV=
OTP_RETURN_TO_CLIENT=false

# OpenTelemetry (optional). When set, gRPC server exports to the deploy stack collector.
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=ztcp-grpc

#
# --- frontend (frontend/.env) ---
BACKEND_GRPC_URL=localhost:8080
# NEXT_PUBLIC_DEFAULT_ORG_ID=
DEV_OTP_ENABLED=false
NEXT_PUBLIC_DEV_OTP_ENABLED=false
NEXT_PUBLIC_DOCS_URL=http://localhost:3001
