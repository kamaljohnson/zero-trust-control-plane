# Grafana provisioning (local deploy)

Dashboard JSON under `provisioning/dashboards/ztcp/` is synced from **`dashboards/ztcp-telemetry-dashboard.json`**. To update the provisioned dashboard after changing the source, copy that file into `provisioning/dashboards/ztcp/ztcp-telemetry.json`.
